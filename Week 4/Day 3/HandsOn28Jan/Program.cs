class Program {
    public static void Main(string[] args) {
        // Donations
        // int n = int.Parse(Console.ReadLine());
        // string[] arr = new string[n];
        // for(int i=0; i<n; i++) {
        //     arr[i] = Console.ReadLine();
        // }
        // int num = int.Parse(Console.ReadLine());
        // int ans = UserProgramCode.getDonations(arr, num);
        // Console.WriteLine("Total Donations are : " + ans);


        // Max difference in array
        // int n = int.Parse(Console.ReadLine());
        // int[] arr = new int[n];
        // for (int i = 0; i < n; i++)
        // {
        //     arr[i] = int.Parse(Console.ReadLine());
        // }
        // int result = MaxDifference.diffIntArray(arr);
        // Console.WriteLine(result);

        // List the elements A
        // int n = int.Parse(Console.ReadLine());
        // List<int> list = new List<int>();
        // for (int i = 0; i < n; i++)
        // {
        //     list.Add(int.Parse(Console.ReadLine()));
        // }
        // int input2 = int.Parse(Console.ReadLine());
        // List<int> output = ListOfElements.GetElements(list, input2);
        // if (output.Count == 1 && output[0] == -1)
        // {
        //     Console.WriteLine("No element found");
        // }
        // else
        // {
        //     foreach (int num in output)
        //     {
        //         Console.Write(num + " ");
        //     }
        // }
    }
}